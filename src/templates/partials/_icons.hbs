<!-- SVG Definitions
To create new SVG symbols, copy and paste vector shapes from Illustrator or
SVG code from another program. Give each symbol a unique id and update the viewBox.
Each SVG symbol should have a matching style to set the aspect ratio
in the .icon CSS object. Use the <use> element to display the icons.
 -->

<svg class="u-isVisuallyHidden">
     <symbol id="icon-default" viewBox="0 0 100 100">
        <path d=""/>
    </symbol>
</svg>
